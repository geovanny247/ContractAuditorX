services:
  - type: web
    name: alchemy-listener
    env: python
    region: oregon
    plan: free
    buildCommand: ""
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    rootDir: src/listener
    envVars:
      - key: ALCHEMY_API_KEY
        value: your-alchemy-key-here
